export default function ManageLoginLayout({ children }) {
    return (
        <div className="manage-login-frame">
            <main className="manage-login-main">
                {children}
            </main>
        </div>
    );
}
