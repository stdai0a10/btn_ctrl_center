# Codex

## step 1

依照文件 `docs/requirements/poc/03-service_manager.md` 裡的規格、建議，進行「管理功能」的開發:

- 此專案後端使用 Laravel 12
- 此專案前端使用 Inertia + React
- 每完成 `Phase 分割建議` 裡的一個階段就進行一次提交

## step 2

幫我調整 `/manage/*` 的前端畫面佈局，如圖片所示：

1. 登入前的 `/manage/login`
2. 登入後的 `/manage/*`，側邊欄展開形式
3. 登入後的 `/manage/*`，側邊欄收合形式

## step 3
